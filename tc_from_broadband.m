function out = tc_from_broadband(edf_path,params)
% convert longitudinal EEG data into TC time series (per 120s window, step
% 30 s)
% - bandpass EEG (0.5 - 40 Hz) for each bins
% - compute the broad-band fluctuations in short bins (powWin_sec)
end